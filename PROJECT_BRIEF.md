# 소상공인 폐업(이탈) 예측 프로젝트 — 확정 사항 정리

> 2차 미니 프로젝트 (원 과제: 가입 고객 이탈 예측 → 소상공인 폐업 예측으로 재해석)
> 담당 역할: 데이터 수집 및 전처리

---

## 1. 프로젝트 프레임

| 원래 개념 (가입 고객 이탈) | 치환 개념 (소상공인 폐업) |
|---|---|
| 고객 | 개별 사업장(점포) |
| 이탈(Churn) | 폐업 |
| 가입기간(tenure) | 존속기간 (개업일자 ~ 폐업일자 또는 현재) |

기존 OTT 이탈 예측 후보는 선배 기수 중복이 많고 데이터가 소진되어 폐기. 소상공인 폐업 쪽은 개별 점포 단위 데이터라 원 과제("가입 고객" 단위 이진분류)에 훨씬 정확히 대응됨.

---

## 2. 타깃(y) 정의 — 확정

- **y = 1 (이탈/폐업)**: 폐업일자가 **2024년 이후**인 경우
- **y = 0 (생존)**: 현재 영업 중인 경우
- **개업일자는 제한하지 않음** — 존속기간(tenure)이 다양하게 분포되도록 전체 포함
- ⚠️ 개업일자만 최근으로 제한하는 방식은 "관찰기간 부족(censoring)"으로 인한 생존 편향이 발생하므로 사용하지 않음 (검토 후 기각)
- **2024년 이전에 이미 폐업한 행은 데이터셋에서 제외** (2026-08-23 확정, `load_and_clean()`에 반영) — 업종밀집도·대중교통 접근성 같은 피처가 "오늘" 기준으로 계산되는데, 오래전에 폐업한 가게에 현재 시점 피처를 붙이면 실제 폐업 당시 환경과 무관한 값을 학습시키게 됨. 이 필터링 전엔 y=0에 "2024년 이전 폐업"까지 섞여 있어서(전체의 60%) y=0이 사실상 "생존"을 의미하지 않는 문제가 있었음

---

## 3. 업종 범위 — 3개 확정, 1개 미정 + 소매업 확장 검토

정부의 "소상공인 주요 6대 업종"(제조/도매/소매/음식/숙박/서비스) 분류를 근거로 선정. 제조업·도매업은 일반 인허가 대상이 아니라(사업자등록만으로 운영 가능) 통합 데이터가 없어 제외. 소매업도 원래는 같은 이유로 제외했으나, 아래 3-1절의 대안 데이터로 확장 검토 중.

### 확정 (2개)

| 업종 | 데이터셋명 | 링크 |
|---|---|---|
| 음식업 | 전국일반음식점표준데이터 | https://www.data.go.kr/data/15096283/standard.do |
| 숙박업 | 행정안전부_문화_숙박업 | https://www.data.go.kr/data/15044968/fileData.do |

### 서비스업 — 확정: 4개 후보 전부 사용

| 업종 | 데이터셋명 | 링크 | 비고 |
|---|---|---|---|
| 미용업 | 전국미용업소표준데이터 | https://www.data.go.kr/data/15096295/standard.do | 기본 후보 |
| 세탁업 | 전국세탁업소표준데이터 | https://www.data.go.kr/data/15114132/standard.do | 공중위생영업 계열 |
| 목욕장업 | 행정안전부_생활_목욕장업 | https://www.data.go.kr/data/15045082/fileData.do | 대중목욕탕·찜질방·사우나 |
| PC방(인터넷컴퓨터게임시설제공업) | 행정안전부_문화_인터넷컴퓨터게임시설제공업 | https://www.data.go.kr/data/15045073/fileData.do | 최근 폐업 트렌드 스토리텔링에 유리 |

4개 중 하나만 고르는 대신 전부 사용하기로 확정(2026-08-22). 파이프라인(`load_and_clean`)이 재사용 가능해 추가 비용이 적고, "서비스업" 전체를 더 잘 대표함. 단, 폐업 패턴이 업종별로 다를 수 있어 모델링 시 업종 구분 변수를 반드시 포함할 것.

### 지역 범위

- **전국** 대상이나, **파일럿은 서울만 우선 다운로드** (전국 파일은 업종당 수백MB로 큼)
- localdata.go.kr 데이터다운로드 메뉴에서 지역 단위 선택 다운로드 가능

### 3-1. 소매업(문구점 등) 확장 — 서울 파일럿 완료

문구점·가챠샵·무인샵·휴대폰액세서리점처럼 인허가 없이 사업자등록만으로 운영 가능한 일반 소매업은 위 확정 업종 데이터에 포함되지 않음. 아래 대안으로 확장 검토 중.

**대안 데이터: 소상공인시장진흥공단 상가(상권)정보**
- 국세청/카드사 기반, 인허가 여부와 무관하게 실제 영업 중인 모든 상가를 포착
- 분류: 대분류 10개 / 중분류 75개 / 소분류 247개 (표준산업분류 기반) — 문구용품·완구 소매업 등 세부 코드 존재
- 주요 컬럼: 상가업소번호, 상호명, 주소, 상권업종명, 표준산업분류명, 경도, 위도
- **한계**: 폐업일자가 직접 제공되지 않음 (행안부 인허가정보 기반이 아닌 항목은 현장조사/카드사 데이터 시점 기준이라 시계열 분석에 부적합하다고 소진공이 공식 안내함)

**우회 방법: 스냅샷 두 시점 비교 — 확정**
- data.go.kr에 "소상공인시장진흥공단_상가(상권)정보_YYYYMMDD" 형태로 시점별 스냅샷이 개별 등록됨
- 실제 확보한 스냅샷은 6개월 간격(03/09월이 아닌 06/12월 기준)으로만 존재: `20240630`, `20241231`, `20250630`, `20251231`, `20260630` (`data/raw/commercial_YYYYMMDD/`에 지역별 CSV로 저장, 아직 안 쓰는 3개는 보류 상태로 남겨둠)
- **최종 확정: `20250630` ↔ `20260630` (정확히 1년 간격)** — 둘 다 2023년 2월 업종분류 개편 이후라 `상가업소번호` 매칭 안전
- 매칭 키: `상가업소번호`. 폐업 추정 로직: 시점 A(20250630)의 상가업소번호가 시점 B(20260630)에 없으면 "폐업 추정"(y=1)

**서울 파일럿 결과 (2026-08-23 완료)** — `src/retail_snapshot.py`의 `estimate_closure()`로 구현, `data/processed/retail_*_seoul_clean.csv` + 통합본 `retail_seoul_clean.csv`(13,599행)

| 업종 | 카테고리/필터 방식 | 시점A 표본 | 폐업 추정(y=1) | 비율 |
|---|---|---|---|---|
| 편의점 | 상권업종소분류명='편의점' | 9,544건 | 1,407건 | 14.7% |
| 폰케이스판매점 | 상권업종소분류명='핸드폰 소매업' (정확한 "폰케이스" 카테고리 없어 근접 카테고리로 대체) | 3,694건 | 468건 | 12.7% |
| 무인매점 | 상호명에 "무인" 포함 (카테고리 자체가 없어 상호명 키워드 필터) | 268건 | 48건 | 17.9% |
| 인형뽑기 | 상호명에 "뽑기"/"클로우" 포함 (카테고리 자체가 없어 상호명 키워드 필터) | 93건 | 13건 | 14.0% |

⚠️ 무인매점·인형뽑기는 표본이 작아서(서울 기준 각 268/93건) 전국으로 확장해도 다른 업종보다 절대 표본 수가 작을 가능성이 높음. 모델링 시 감안 필요.

**남은 할 일**
1. ~~스냅샷 목록 확인~~ 완료
2. ~~두 시점 확정~~ 완료 (20250630↔20260630)
3. ~~서울 파일럿 → 상가업소번호 매칭 테스트~~ 완료
4. 전국 확장 (현재 서울만 처리됨)
5. 상호명 유사도 체크(선택) — 번호는 사라졌지만 같은 자리에 비슷한 이름으로 재등록된 경우가 있는지 샘플 확인해 오탐률 가늠

**유의: 데이터 신뢰도 이질성**

기존 3개 업종(정확한 폐업일자) vs 소매업(스냅샷 비교로 추정)은 신뢰도 성격이 다름. 전처리 결과서에 "왜 업종별로 폐업 판정 방식이 다른지" 명시 필요.

> "소매업(문구·완구 등) 카테고리는 인허가 대상이 아니라 정확한 폐업일자가 없어, 2024-03-31과 2026-XX-XX(확정 예정) 두 시점 스냅샷 비교로 폐업 여부를 추정하였다. 이는 실제 폐업일자가 아닌 근사치이며, 상호명/주소 표기 변경 등으로 인한 오차 가능성을 포함한다."

---

## 4. 데이터 형식 / 기술 노트

- **인코딩**: 국내 공공데이터 CSV는 `cp949`(구 euc-kr)인 경우가 많음 — 읽기 실패 시 이걸 우선 시도
- **좌표계**: `Bessel 중부원점TM (EPSG:5174)` — 위경도(WGS84)로 변환 필요 (지도 UI용). `pyproj` 라이브러리 사용
- **파일 규모(참고)**: 전국일반음식점표준데이터는 100만 건 이상 (누적 개업+폐업 이력). 엑셀 프로그램으로 열면 다 안 보이므로 반드시 pandas로 직접 읽을 것

---

## 5. 전처리 파이프라인 설계 순서

1. **업종 1개(일반음식점, 서울)로 파이프라인 전체를 먼저 완성** — 나머지 업종에 재사용할 "틀"을 만드는 목적
2. 다운로드 → 인코딩 확인 → 컬럼 구조 파악
3. 업종별 컬럼명 차이 확인 → 공통 표준 스키마로 매핑 (개업일자, 폐업일자, 영업상태, 업종명, 소재지주소, 좌표)
4. 타깃(y) 생성 (2번 규칙 적용)
5. 결측치/이상치 탐색 (개업일자 > 폐업일자 같은 논리 오류, 좌표 결측/이상값 등)
6. 파생변수 생성: 존속기간(tenure), 개업연도, 좌표 변환(TM→위경도)
7. 위 로직을 함수화(`load_and_clean(파일경로, 업종명)` 형태)해서 나머지 업종에 재적용
8. 업종별 데이터프레임을 `pd.concat`으로 통합

---

## 6. 확장 후보 데이터 (2차, 우선순위별)

파일럿(3개 업종 전처리) 완료 후 시간이 남으면 추가 검토. 처음부터 전부 연결하면 핵심 작업(모델링) 시간이 부족해질 위험이 있어 후순위로 둠.

### 우선순위 높음
- **업종 밀집도(경쟁강도)**: 외부 데이터 불필요, 보유 데이터에서 반경 300m 내 동일업종 점포 수를 직접 계산 — **전국 단위로 계산 진행 중** (2026-08-23, `src/features.py`의 `add_industry_density()`, BallTree(haversine) 공간 인덱싱 사용. 300만 행 규모라 순수 pandas 반복은 비현실적)
- **프랜차이즈 여부**: 공정거래위원회 "페어데이터" 가맹점 위치정보(좌표 포함) — https://www.data.go.kr/data/15143698/openapi.do — **OpenAPI 활용신청 승인 대기 중이라 보류**

### 우선순위 중간
- 유동인구/생활인구: 서울 열린데이터광장, 소진공 상권정보 API
- **대중교통 접근성**: 전국 버스정류장·지하철역 좌표 (data.go.kr) — **원본 확보 완료, 최근접 거리(m) 계산 진행 중** (`data/raw/bus_stops_nationwide.csv` cp949, `data/raw/subway_stations_nationwide.xlsx`). `src/features.py`의 `add_transit_accessibility()` — 버스/지하철 각각 BallTree로 최근접 거리 계산
- 인구통계(연령·가구수): 통계청 인구총조사

### 우선순위 낮음 (권장 안 함)
- 최저임금, 소비자심리지수 — 전국 단일값이라 지역/개별 점포 간 변별력 낮음

### 보류 (참고만)
- **임대료/공실률**: 한국부동산원 "상업용부동산 임대동향조사" (소규모상가 기준 권장)
  - 임대료: https://www.data.go.kr/data/15069789/fileData.do
  - 공실률: https://www.data.go.kr/data/15069735/fileData.do
  - 분기·시군구 단위 집계 데이터라 개별 점포와 join하려면 소재지주소에서 시군구 추출 후 매칭 필요

---

## 7. 모델링 방향 (참고용 — 데이터 담당 범위 밖이지만 전처리 설계에 영향)

CNN 등 데이터 형태에 안 맞는 모델은 사용하지 않음(표 데이터이므로). "성능 vs 해석력" 트레이드오프를 스토리 축으로 삼음.

| 단계 | 목적 | 사용 모델 |
|---|---|---|
| 예측 | 폐업 여부 분류 | 로지스틱회귀 → 랜덤포레스트 → MLP (복잡도 순 비교) |
| 원인분석 | 폐업 요인 해석 | 로지스틱회귀 계수, 랜덤포레스트 feature importance (MLP는 해석력 낮아 제외) |
| 전략제안 | 도메인 연결 | 서울시 "우리마을가게 골목상권" 벤치마킹 (golmok.seoul.go.kr) |
| 검증 | 트레이드오프 정리 | 성능표 + "MLP가 성능은 좋지만 해석은 안 된다" 같은 결론 |

### 벤치마크 참고 사이트
- **golmok.seoul.go.kr**: "나도 곧 사장" 자가진단 기능 — 순수익(매출-임대료-인건비) 기반 폐업 위험도 예측, 실제 개업/폐업일자로 지도학습. 저희 프로젝트의 목표 UI/로직과 가장 유사한 실제 사례.

---

## 8. TODO (미확정 사항)

- [x] 서비스업 최종 업종 확정 — 4개(미용업/세탁업/목욕장업/PC방) 전부 사용하기로 확정 (2026-08-22)
- [x] 파일럿(일반음식점 서울) 실제 다운로드 및 구조 확인 — 완료. `src/preprocessing.py`의 `load_and_clean()` 참고
- [x] 6개 업종(일반음식점·숙박업·미용업·세탁업·목욕장업·PC방) 전국 데이터 전처리 및 통합 완료 (2026-08-22) — `data/processed/all_industries_clean.csv`, 전체 2,974,914행, y=1(폐업) 239,020건(8.0%). 서울 파일럿에서 검증된 파이프라인을 그대로 전국 확장에 적용 — 로컬 처리로 충분히 처리됨(Colab 불필요했음)
- [x] 소매업 확장 스냅샷 텀 확정 및 서울 파일럿 완료 (2026-08-23) — `20250630↔20260630`(1년), 4개 카테고리(편의점/폰케이스판매점/무인매점/인형뽑기) 결과는 3-1절 참고
- [x] 업종 밀집도·대중교통 접근성 피처 — 전국 단위 계산 완료 (2026-08-23, `scripts/add_features.py`). 밀집도는 반경 300m 내 "현재 영업 중인" 동일업종 점포 수만 카운트(폐업 이력 포함하면 크게 부풀려져서 활성 점포로 한정)
- [x] 2024년 이전 폐업 건을 데이터셋에서 제외 (2026-08-23) — y=0이 "생존"만 의미하도록 정리. 2장/10장 참고. 전체 행 수 2,974,914 → 1,191,979로 변경됨
- [ ] 소매업 전국 확장 (현재 서울만 처리됨)
- [ ] 프랜차이즈 여부 — 공정위 OpenAPI 승인 대기 중이라 보류
- [ ] 유동인구/생활인구, 인구통계 등 중간 우선순위 확장 데이터는 미착수

---

## 9. 사용 패키지 / 저장소 구조 (2026-08-23 기준)

### 개발 환경
- Python 3.12, conda 가상환경 `skn35-3team`
- 주요 패키지(`requirements.txt`): `pandas`, `numpy`, `pyproj`(좌표계 변환), `openpyxl`(xlsx 읽기), `scikit-learn`(BallTree 공간 인덱싱 — 밀집도/최근접거리 계산에 사용)

### 저장소 구조
```
src/
├── preprocessing.py    # load_and_clean() — 인허가 데이터(6개 업종) 표준 전처리
├── retail_snapshot.py  # estimate_closure() — 소매업 스냅샷 비교 폐업 추정
└── features.py          # add_industry_density(), add_transit_accessibility() — 공간 피처

scripts/
├── run_all.py            # 6개 인허가 업종 전국 전처리 + 통합 실행
├── run_retail_pilot.py   # 소매업 4개 카테고리 서울 파일럿 실행
├── add_features.py       # 밀집도/대중교통 피처 계산 실행
├── check_columns.py      # 신규 원본 파일 컬럼 스키마 비교용
└── inspect_*.py          # 원본 파일 인코딩/컬럼/카테고리 확인용 1회성 스크립트

data/           # git에 커밋 안 함(용량), data/README.md에 받는 법 정리
├── raw/        # 원본 다운로드 파일 (영문명으로 통일)
└── processed/  # 전처리 결과물
```

### 현재 산출물 (`data/processed/`)
- `all_industries_clean.csv` — 6개 인허가 업종 전국 통합, 2024년 이전 폐업 건 제외 (1,191,979행)
- `retail_seoul_clean.csv` — 소매업 4개 카테고리 서울 (13,599행)
- `all_industries_features.csv`, `retail_seoul_features.csv` — 위 두 파일에 업종밀집도·대중교통 접근성 컬럼 추가. **모델링에는 이 두 파일(`_features.csv`)을 쓸 것** — `_clean.csv`는 피처 추가 전 중간 산출물

---

## 10. 정제 데이터 컬럼 설명 (데이터 사전)

팀원이 모델링에 바로 쓸 수 있도록 최종 산출물 컬럼을 정리. `_clean.csv`는 아래 표에서 밀집도/대중교통 3개 컬럼만 빠진 버전.

### 데이터 품질 보정 (2026-08-23)

`data/processed/*` 전체를 감사(`scripts/audit_quality.py`)해서 찾은 문제 중 실제로 고칠 필요가 있는 것만 `src/preprocessing.py`의 `load_and_clean()`에 반영 — 파이프라인을 다시 돌려도 항상 같은 보정이 적용됨.

- **지오코딩 오류 좌표 2건** → 좌표X/좌표Y/경도/위도를 결측 처리. (예: 서로 다른 상호의 두 업체가 완전히 동일한, 대한민국 범위(위도 33~39, 경도 124~132) 밖 좌표를 갖고 있던 사례) `KOREA_LAT_RANGE`/`KOREA_LON_RANGE` 상수로 판정
- **개업일자 placeholder 의심 120건(1950년 이전)** → 원본 개업일자는 보존하되 `tenure_days`/`tenure_years`/`개업연도`만 결측 처리(23건이 정확히 `1900-05-31`로 동일 — 실제 개업일이 아니라 "날짜 모름" 기본값으로 추정). `PLACEHOLDER_YEAR_THRESHOLD` 상수로 판정. 보정 후 tenure_years 최댓값 126.6년 → 74.8년
- **2024년 이전 폐업 건 제외** (2026-08-23) → 전체 2,974,914행 중 1,782,935건(60%)이 "이미 2024년 이전에 폐업"인데 y=0(생존)으로 잡혀 있던 문제를 발견. 업종밀집도/대중교통 접근성 피처가 "오늘" 기준값이라 오래전 폐업 건에 붙이면 시점이 안 맞아 제외하기로 결정. 제외 후 **1,191,979행**(y=1 239,020건, 20.1%)

**의도적으로 손대지 않은 것** (정상인데 극단값처럼 보였던 케이스, 조사 후 원값 유지):
- 좌표 결측 20.5만 건(6.9%) — 원본 자체에 좌표 없음, 임의로 채우지 않음
- 폐업일자 결측 95만 건 — "현재 영업 중"이라는 정상적 의미, 결측 아님
- 최근접버스정류장_거리m 최댓값 142km(백령도), 최근접지하철역_거리m 최댓값 228km(제주 가파도) — 실제 외딴섬 소재지라 정상
- 업종밀집도 최댓값 877(부산 부전동/서면 상권) — 실제 고밀도 상업지구라 정상
- 소매업 데이터의 상가업소번호 9건 중복 — 상호명이 "무인"도 포함하면서 편의점 카테고리에도 속하는 등 카테고리 필터가 겹치는 게 의도된 설계라 그대로 둠(두 파일 합쳐서 "가게 개수" 셀 때는 유의)

### 10-1. `all_industries_features.csv` — 인허가 6개 업종 전국 (1,191,979행)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| 업종명 | 문자열 | `일반음식점`/`숙박업`/`미용업`/`세탁업`/`목욕장업`/`PC방` 중 하나 |
| 사업장명 | 문자열 | 상호명 |
| 개업일자 | 날짜(YYYY-MM-DD) | 인허가일자 |
| 폐업일자 | 날짜(YYYY-MM-DD) | 현재 영업 중이면 결측(NaN) |
| 영업상태 | 문자열 | `영업/정상` 또는 `폐업` |
| 소재지주소 | 문자열 | 지번주소 우선, 없으면 도로명주소로 보완 |
| 좌표X, 좌표Y | 실수 | 원본 좌표(EPSG:5174, Bessel 중부원점TM) — 보통 안 씀, 검증용으로만 남겨둠 |
| 경도, 위도 | 실수 | 좌표X/Y를 WGS84로 변환한 값. **지도/거리계산엔 이 컬럼 사용** |
| tenure_days, tenure_years | 실수 | 존속기간 = (폐업일자 또는 오늘) − 개업일자 |
| 개업연도 | 정수 | 개업일자의 연도 |
| **y** | 정수(0/1) | **타깃.** 1 = 폐업일자가 2024-01-01 이후(이탈), 0 = 그 외(현재 영업 중이거나 2024년 이전 폐업) |
| 업종밀집도 | 정수 | 이 지점 반경 300m 내, **같은 업종명이면서 현재 영업 중인(영업상태=='영업/정상')** 점포 수(자기 자신 제외). 경쟁강도 프록시 |
| 최근접버스정류장_거리m | 실수 | 가장 가까운 버스정류장까지 직선거리(m) |
| 최근접지하철역_거리m | 실수 | 가장 가까운 지하철역까지 직선거리(m) |

**결측 있는 컬럼**: 폐업일자(현재 영업 중인 경우 정상적인 결측), 좌표X/Y·경도·위도(원본 자체 결측 + 지오코딩 오류 보정 2건), 업종밀집도·거리 컬럼(좌표 결측인 행은 계산 불가라 함께 결측, 1,151,966/1,191,979행에 값 있음), tenure_days/tenure_years/개업연도(개업일자 자체 결측 + placeholder 의심 케이스).

### 10-2. `retail_seoul_features.csv` — 소매업 4개 카테고리, 서울만 (13,599행)

인허가 대상이 아니라 정확한 개업/폐업일자가 없음 — **스냅샷 두 시점(2025-06-30 vs 2026-06-30) 비교로 폐업을 추정**한 결과라 위 파일과 신뢰도 성격이 다름(자세한 배경은 3-1절).

| 컬럼 | 타입 | 설명 |
|---|---|---|
| 업종명 | 문자열 | `편의점`/`폰케이스판매점`/`무인매점`/`인형뽑기` 중 하나 |
| 사업장명 | 문자열 | 상호명 |
| 소재지주소 | 문자열 | 지번주소 우선, 없으면 도로명주소로 보완 |
| 경도, 위도 | 실수 | WGS84 (원본에 이미 경위도로 제공됨, 별도 변환 불필요) |
| 상가업소번호 | 문자열 | 원본 식별자. 두 시점 스냅샷 매칭에 쓰인 키 (join할 때 유용) |
| **y** | 정수(0/1) | **타깃.** 1 = 2025-06-30 시점엔 있었지만 2026-06-30 시점에 없음(폐업 추정), 0 = 2026-06-30 시점에도 존재(생존 추정). ⚠️ 실제 폐업일자가 아닌 근사치 — 업종 재분류·상호명 변경 등으로 인한 오탐 가능성 있음 |
| 업종밀집도 | 정수 | 반경 300m 내, 같은 업종이면서 **2026-06-30 시점에 생존한(y==0)** 점포 수 |
| 최근접버스정류장_거리m | 실수 | 가장 가까운 버스정류장까지 직선거리(m) |
| 최근접지하철역_거리m | 실수 | 가장 가까운 지하철역까지 직선거리(m) |

개업일자/tenure에 해당하는 컬럼이 없음(원본 데이터 자체에 없음) — 두 파일을 합쳐서 모델링할 땐 이 차이를 감안할 것.
