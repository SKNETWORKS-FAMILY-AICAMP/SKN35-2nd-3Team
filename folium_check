"""
프로젝트 루트에서: python check_streamlit_folium.py
streamlit_folium이 실제로 어디서 로드되는지, 내용물이 제대로 들어있는지 확인용.
"""
import streamlit_folium

print("모듈 파일(__file__):", getattr(streamlit_folium, "__file__", None))
print("패키지 경로(__path__):", list(getattr(streamlit_folium, "__path__", [])))
print("st_folium 있음?:", hasattr(streamlit_folium, "st_folium"))
print("가진 이름들:", [n for n in dir(streamlit_folium) if not n.startswith("_")])